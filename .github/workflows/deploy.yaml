name: Deployment

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install and build
        run: |
          npm ci
          npm run build:h5

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: dist-files
          path: ./dist/build/h5

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://kid-learn.wingedge777.com/
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v7
        with:
          name: dist-files
          path: dist

      - name: Setup SSH Key
        env:
          deploy_key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf "%s\n" "$deploy_key" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Deploy via rsync
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_PORT: ${{ secrets.VPS_SSH_PORT }}
        run: |
          rsync -avz \
          --delete \
          -e "ssh -p $VPS_PORT -o BatchMode=yes -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa" \
          dist/ $VPS_USER@$VPS_HOST:/var/www/kid-learn
